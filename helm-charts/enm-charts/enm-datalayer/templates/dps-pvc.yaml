kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: dps-pvc
  labels:
    kubernetes.io/cluster-service: "false"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100M

